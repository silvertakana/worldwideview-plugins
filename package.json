{
  "name": "worldwideview-plugins",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r --filter './packages/wwv-plugin-*' run build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "sync:all": "node local-scripts/sync-all.mjs"
  },
  "devDependencies": {
    "@types/react": "^19",
    "rollup-plugin-external-globals": "^0.13.0",
    "typescript": "^5",
    "vite": "^8.0.8",
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "@worldwideview/wwv-plugin-sdk": "^1.4.10",
    "lucide-react": "^1.8.0"
  }
}
